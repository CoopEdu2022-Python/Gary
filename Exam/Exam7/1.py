def gcd_pro_max_2(*n):
    while
        a=0
        h = sorted(n)
        for i in n:
            a+=i
        if h[0]*len(n)==a:
            return h[0]
        for i,p in enumerate(h):
            if h[i]//h[i+1]==1:
                pass

            else:
                h[i] = h[i] % h[i+1]





